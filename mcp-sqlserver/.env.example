# Copy this file to .env and update values.
# SA password policy (per SQL Server docs):
# - Minimum 8 chars
# - Must include 3 of 4 classes: uppercase, lowercase, digit, symbol
MSSQL_SA_PASSWORD=ChangeMe!234

# SQL Server edition in container. For demo use Developer or Express.
MSSQL_PID=Developer

# Optional host port mapping if 1433 is occupied.
MSSQL_HOST_PORT=1433
